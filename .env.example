# Target domain to forward requests to
TARGET_DOMAIN=https://example.com

# Proxy server configuration
PROXY_HOST=0.0.0.0
PROXY_PORT=5000

# Path to the JSON modification rules file (optional)
# Copy json_rules.example.json → json_rules.json and edit to enable request modifications
JSON_RULES_FILE=json_rules.json
